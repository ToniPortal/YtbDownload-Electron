# electron-builder.yml

appId: com.example.YtbDownload

nsis:
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: false

win:
  target: nsis

mac:
  target: default

linux:
  target: default
