appId: com.example.YtbDownload

files:
  - 'dist/**/*'

nsis:
  oneClick: false
  perMachine: true

win:
  target: nsis

mac:
  target: default

linux:
  target: default

publish:
  provider: github
  token: ${{ secrets.GITHUB_TOKEN }}